@model dynamic

@if (ViewBag.Message is {} message)
{
    <div class="alert alert-info alert-dismissible fade show" role="alert">
        <p>@message</p>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}